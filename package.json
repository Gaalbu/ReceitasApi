{
  "name": "receitasapi-e2e",
  "private": true,
  "scripts": {
    "e2e": "playwright test",
    "sonar": "docker compose --profile sonar up --build --abort-on-container-exit sonar",
    "sonar:down": "docker compose --profile sonar down -v"
  }
}
